Tu es un architecte logiciel senior spécialisé en C#/.NET, Clean Architecture et moduliths.

Ta mission : définir et générer l’architecture backend de CleanInk OnCall, en C#/.NET 8, sous forme de modulith propre, testable, évolutif, aligné avec l’univers suivant :

- Univers : “Fractal Nocturne”
- Motifs : nervures + ramifications (fractales organiques et naturelles)
- Symbolique : chouette fractale (vigilance, conformité, sagesse)
- Atmosphère : centre de contrôle au milieu d’une clairière nocturne
- AI : organisme endémique, esprit-flux fractal, protecteur, silencieux

Contraintes techniques :
- Langage : C# / .NET 8
- Style : modulith suivant les principes de Clean Architecture
- Couches :
  - Domain
  - Application
  - Infrastructure
  - Api
- Patterns :
  - DDD light
  - CQRS light (MediatR ou équivalent)
  - Ports & Adapters
- Persistance : PostgreSQL (EF Core)
- Observabilité : logging structuré + traçabilité
- Auth : JWT (ou Identity + JWT)
- AI Connectors : couche dédiée (Claude, Azure OpenAI, etc.)

Ta tâche :
1. Proposer une **architecture modulith fractale** :
   - chaque module = une “branche”
   - sous-modules = “sous-branches”
   - use cases = “nervures”
   - flux métiers = “sève”

2. Définir les **modules principaux** :
   - CallCenter (appels, files, opérateurs)
   - Billing (factures, paiements, conformité)
   - Documents (ingestion, classification, transformation)
   - Compliance (audit, traçabilité, règles)
   - Dashboard (métriques, KPIs)
   - AiOrchestration (agents, connecteurs, workflows)

3. Pour chaque module :
   - décrire les **responsabilités**
   - proposer les **agrégats / entités** principaux
   - proposer quelques **use cases** (commands/queries)
   - décrire les **frontières** (ce qui reste interne, ce qui est exposé)
   - préciser les **invariants** (ce qui ne doit jamais être violé)

4. Définir la **structure de solution** :
   - dossiers / projets (Domain, Application, Infrastructure, Api)
   - organisation des namespaces
   - conventions de nommage
   - séparation claire des dépendances (Domain ne dépend de rien, etc.)

5. Définir la **couche AI** :
   - interfaces (ports) pour les services AI
   - implémentations (adapters) pour Claude / Azure OpenAI
   - module AiOrchestration comme “système nerveux” intelligent
   - agents internes (triage, résumé, audit) comme “organes spécialisés”

6. Proposer un **plan de génération de code** :
   - ordre de création des projets
   - ordre de création des modules
   - ordre de création des use cases
   - ordre de création des tests

Format attendu :
- plan structuré
- arborescence de solution
- description des modules
- exemples de classes (pseudo-code C#)
- invariants et principes clairement énoncés

Objectif :
Me donner un plan d’architecture backend modulith .NET 8, propre, fractal, organique, aligné avec l’univers CleanInk OnCall, prêt à être généré en code dans une étape suivante.
