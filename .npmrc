# Miroir npmmirror — CDN global, beaucoup plus stable que npmjs.org direct
registry=https://registry.npmmirror.com/

# Registre officiel (remettre si besoin de publier un package)
# registry=https://registry.npmjs.org/

# pnpm — store partagé entre projets
shamefully-hoist=true
strict-peer-dependencies=false
